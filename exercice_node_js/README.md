To run this wondefull project :

1. npm install on the folder root
2. nodemon app.js

If, for some unbelivable reason you don't have nodemon, make sure to install it first.

try to google "How to install nodemon ?" and then do it.